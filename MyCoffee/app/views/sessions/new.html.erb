<%= simple_form_for :user, 
    :url => url_for(:action => 'create', :controller => 'sessions'),
    :method => 'post' do |f| %>
    	<%= f.input :email %>
    	<%= f.input :password %>
  <%= f.button :submit %>
<% end %>
